# Copy to .env.local for local dev, and set the same vars in Vercel → Settings → Environment Variables.

# Canonical site URL (no trailing slash). Drives canonical tags, OG/Twitter URLs,
# JSON-LD, sitemap, robots, and RSS. MUST be set in production.
NEXT_PUBLIC_SITE_URL=https://ibnads.com
